create table fsd_test_contacts( user_id int, name varchar(150), email_id varchar(100), mobile int, age int, d_o_b date, location text );

create table contacts( user_id int, fisrt_name varchar(150), last_name varchar(150), email_id
varchar(100), mobile int, age int, d_o_b date, location text );
